  {{^is "home"}}
    <a class="backArrow" href="{{@blog.url}}"><img class="back_button" src="/assets/img/back.svg" width="40px" height="40px"></a>
  {{/is}}
  <nav>
    <ul>
      {{#foreach navigation}}
        <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
      {{/foreach}}
    </ul>
  </nav>
